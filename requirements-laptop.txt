# Minimal dependencies for laptop testing (CPU-only)
# Install with: pip install -r requirements-laptop.txt

# Core ML Libraries (CPU versions)
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0

# Evaluation & Metrics
scipy>=1.11.0
scikit-learn>=1.3.0

# Code Execution & Safety
RestrictedPython>=6.0

# Utilities
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.66.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Development & Testing
pytest>=7.4.0
black>=23.0.0

# Jupyter (for analysis)
jupyter>=1.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Note: This excludes heavy dependencies (accelerate, trl, wandb) for faster installation
# These will be needed on NSCC but not for local development/testing
